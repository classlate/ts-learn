let str: string = 'abc'

let num: number = 18

let big: bigint = 123n

let bool: boolean = true

let foo: null = null

let bar: undefined = undefined

let sym: symbol = Symbol('foo')

console.log(str, num, big, bool, foo, bar, sym)
